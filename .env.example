# Base URL of the passoca-api server. Also used by the /roulette page for the
# realtime Socket.IO + Yjs connection (e.g. http://localhost:3000 in dev).
VITE_API_URL="http://localhost:3000"

# Supabase (used by notes / quotes / rss)
VITE_SUPABASE_URL=""
VITE_SUPABASE_KEY=""
